%h2 Actions
%ul
 - if can? :read, Product
  %li= link_to 'Scarves', products_path
 - if can? :read, Role 
  %li= link_to 'Roles', roles_path
 - if can? :read, User 
  %li= link_to 'Users', users_path
