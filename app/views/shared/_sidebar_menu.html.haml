%div{:style=>'margin:5px;position:relative;top:15px;'} 
 %h5 Everyday 
%ul.nav.nav-list.bs-docs-sidenav{:style=>'margin-left:5px;'}  
 - if can? :read, Task
  %li= link_to 'My Tasks', my_tasks_path
 - if can? :read, Customer 
  %li= link_to 'New Order', new_purchase_path
 - if can? :read, Order 
  %li= link_to 'All Orders', orders_path  
  %li= link_to 'Order Items', order_line_items_path
%div{:style=>'margin:5px;position:relative;top:15px;'}
 %h5 Manage 
%ul.nav.nav-list.bs-docs-sidenav{:style=>'margin-left:5px;'}  
 - if can? :read, Customer 
  %li= link_to 'Customers', customers_path
 - if can? :read, Fabric 
  %li
   = link_to 'Fabrics', fabrics_path
  - if can? :create, Fabric         
   %li
    = link_to 'New Fabric', new_fabric_path
 - if can? :read, Product
  %li= link_to 'Products', products_path
%div{:style=>'margin:5px;position:relative;top:15px;'}
 %h5 Other 
%ul.nav.nav-list.bs-docs-sidenav{:style=>'margin-left:5px;'}  
 - if !current_user.nil? && current_user.username == 'joel'
  - if can? :read, Version
   %li= link_to 'Versions', versions_path
  - if can? :read, AddressType 
   %li= link_to 'Addr Types', address_types_path
  - if can? :read, CustomerAddress
   %li= link_to 'Cust Addrs', customer_addresses_path
 - if can? :read, OrderStatus 
  %li= link_to 'Order Statuses', order_statuses_path
 - if can? :read, Task
  %li
   = link_to 'Tasks', tasks_path
  - if can? :create, Task
   %li
    = link_to 'New Task', new_task_path
 - if can? :read, TaskStatusType 
  %li= link_to 'Task Status Types', task_status_types_path
 - if can? :read, Note 
  %li= link_to 'Notes', notes_path
 - if can? :read, Retailer 
  %li= link_to 'Retailers', retailers_path
 - if can? :read, Farm 
  %li= link_to 'Farms', farms_path
 - if can? :read, Address 
  %li= link_to 'Addresses', addresses_path
%ul.nav.nav-list.bs-docs-sidenav{:style=>'margin-left:5px;'}  
- if !current_user.nil? && current_user.username == 'joel'
 %h5 Joel Only 
 %ul
  - if can? :read, Role 
   %li= link_to 'Roles', roles_path
  - if can? :read, User 
   %li= link_to 'Users', users_path
  - if can? :create, Product
   %li= link_to 'Payment', :payment 
