#scan_column
 = render 'shared/sidebar_menu'
#content 
 %h1 Listing customers
 
 %table
  %tr
   %th Name 
   %th Email
   %th Description
   - if can? :create, Note 
    %th
    %th
   - if can? :create, Order 
    %th
 
  - @customers.each do |customer|
   %tr
    - if can? :read, Customer
     %td
      = link_to customer.fullname, customer
      - # - if can? :update, Customer
      - #  = link_to 'Edit', edit_customer_path(customer)
      - # - if can? :destroy, Customer
      - #  = link_to 'Destroy', customer, :method => :delete, :data => { :confirm => 'Are you sure?' }
    - else
     %td= customer.fullname
    %td= customer.email
    %td= customer.description
    - if can? :create, Customer 
     %td= link_to 'Fyi', new_note_path
     %td= link_to 'Todo', new_note_path
    - if can? :create, Order 
     %td= link_to 'New Order', new_customer_order_path(customer)
 
 %br
 - if can? :create, Customer 
  = link_to 'New Customer', new_customer_path
