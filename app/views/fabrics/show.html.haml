%div.span2
 = render 'shared/sidebar_menu'
%div.span10
 %p
  %b Name:
  = @fabric.name

 - if can? :edit, Fabric
  = link_to 'Edit', edit_fabric_path(@fabric)
 - if can? :read, Fabric
  \|
  = link_to 'Back', fabrics_path
 %br
 %br
 %br
 = render 'prices_list'
