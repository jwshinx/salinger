%div.span2
 = render 'shared/sidebar_menu'
%div.span10
 %h1 Editing fabric
 %span.small_red_italics (To enter a new price, toggle the link at right.)
 = render 'form'
 - if can? :read, Fabric
  = link_to 'Show', @fabric
  \|
  = link_to 'Back', fabrics_path
