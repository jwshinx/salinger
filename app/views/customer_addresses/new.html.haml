#scan_column
 = render 'shared/sidebar_menu'
#content_area
 %h1 
  New Address:
  = link_to "#{@customer.fullname}", customer_path(@customer)

 = render 'form'

 = link_to 'Back', customer_addresses_path
